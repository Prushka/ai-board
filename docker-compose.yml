services:
  translator:
    build:
      context: .
      dockerfile: Dockerfile
    image: meinya/ai-translator:latest
    container_name: ai-translator
    restart: unless-stopped
    ports:
      - "8872:3000"
    environment:
      - NODE_ENV=production
      # - OPENAI_ENDPOINTS=[{"name": "Local", "url": "http://host.docker.internal:11434/v1", "key": "ollama", "defaultModel": "llama3"}]
